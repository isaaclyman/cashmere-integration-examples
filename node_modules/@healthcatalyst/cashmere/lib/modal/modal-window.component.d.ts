import { state } from '@angular/animations';
import { ModalSize } from './modal-options';
import { ElementRef } from '@angular/core';
import { ActiveModal } from './active-modal';
export declare class ModalWindowComponent {
    private activeModal;
    private el;
    _ignoreOverlayClick: boolean;
    _size: ModalSize;
    constructor(activeModal: ActiveModal, el: ElementRef);
    _fadeInOut(): typeof state;
    _overlayClick(event: any): void;
    _eventPath(evt: any): any;
}

import { DateRangeOptions, DateRange } from '../model/model';
import { Observable } from 'rxjs';
import { D } from '../../datepicker';
export declare class ConfigStoreService {
    private defaultOptions;
    private dateRangeOptionsSubject;
    dateRangeOptions$: Observable<DateRangeOptions>;
    private rangeUpdateSubject;
    rangeUpdate$: Observable<DateRange>;
    private presetUpdateSubject;
    presetUpdate$: Observable<number | DateRange>;
    weekendFilter: (d: D) => boolean;
    private readonly emptyWeekendFilter;
    private readonly excludeWeekendFilter;
    constructor();
    updateDateRangeOptions(options: DateRangeOptions): void;
    updateRange(dateRange: DateRange): void;
    updatePreset(value: number | DateRange): void;
}

import { OnInit, EventEmitter, OnDestroy, ElementRef, OnChanges, SimpleChanges } from '@angular/core';
import { CalendarOverlayService } from '../services/calendar-overlay.service';
import { DateRange, DateRangeOptions } from '../model/model';
import { ConfigStoreService } from '../services/config-store.service';
export declare class DateRangeDirective implements OnInit, OnDestroy, OnChanges {
    private _elementRef;
    private calendarOverlayService;
    configStoreService: ConfigStoreService;
    /** Emits when date range is been changed. */
    readonly selectedDateRangeChanged: EventEmitter<DateRange>;
    /** Sets the selected date range. Accepts either a `DateRange` or a numerical index for preset. */
    selectedDate: number | DateRange;
    /** Emits either a numerical index for the selected preset, or a `DateRange` if the selected value is not a preset */
    readonly selectedPresetChanged: EventEmitter<number | DateRange>;
    /** Configuration to setup behavior of component. */
    options: DateRangeOptions;
    private _overlayRef;
    constructor(_elementRef: ElementRef<HTMLInputElement>, calendarOverlayService: CalendarOverlayService, configStoreService: ConfigStoreService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    _onClick(): void;
}

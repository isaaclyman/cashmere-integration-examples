import { ElementRef, AfterViewInit } from '@angular/core';
/** Container for scroll navigation links. */
export declare class HcScrollNavComponent implements AfterViewInit {
    _elementRef: ElementRef;
    private linkList;
    readonly _links: Array<HTMLElement>;
    private readonly ACTIVE_CLASS;
    constructor(_elementRef: ElementRef);
    ngAfterViewInit(): void;
    _setActiveClassById(id: string): void;
    private setActiveClass;
}

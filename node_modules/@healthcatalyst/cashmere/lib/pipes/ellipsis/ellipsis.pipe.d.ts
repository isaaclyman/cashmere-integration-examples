import { PipeTransform } from '@angular/core';
export declare type EllipsisMode = 'characters' | 'words';
export declare class EllipsisPipe implements PipeTransform {
    static readonly ELLIPSIS = "\u2026";
    transform(value: string, length: number, mode?: EllipsisMode): string;
    private firstNWords;
    private firstNCharacters;
}

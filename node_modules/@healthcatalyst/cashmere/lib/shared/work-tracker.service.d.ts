import { Observable, Subscription } from 'rxjs';
export declare class WorkTrackerService {
    start(task: () => Promise<any>): Observable<boolean>;
    startObservable(task: () => Subscription): Observable<boolean>;
}

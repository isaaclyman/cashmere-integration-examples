import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
export declare class CellResizeEvent {
    width: number;
    directionLeft: boolean;
    constructor(width: number, directionLeft: boolean);
}
export declare class HcCellResizer implements OnInit, OnDestroy {
    /**
     * Sets whether the resizer is active or not
     */
    disabled: boolean;
    /**
     * Stores the width value for this resizer;
     * typically used with two-way binding on the cell's style.width property
     */
    width: number;
    private _width;
    private _directionModifier;
    private _mouseX;
    /**
     * Emits a `CellResizeEvent` when a cell has been resized
     */
    resized: EventEmitter<CellResizeEvent>;
    private isResizing;
    private isResizing$;
    /**
     * Emits a boolean value of true while a cell is being resized
     */
    resizing: EventEmitter<boolean>;
    private dragSubscription?;
    ngOnInit(): void;
    private _resizeColumn;
    private _stopResizing;
    _startResizing(event: MouseEvent): void;
    ngOnDestroy(): void;
}

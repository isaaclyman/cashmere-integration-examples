import { FilterableSelectList, SelectListOption } from '../pane/picklist-pane.model';
export declare class PicklistFilterLocalService {
    filter<T extends SelectListOption>(list: FilterableSelectList<T>, searchTokens: string[]): void;
    itemHasSearchTokens<T extends SelectListOption>(list: FilterableSelectList<T>, item: T, searchTokens: string[]): boolean;
    private getSortFunc;
}

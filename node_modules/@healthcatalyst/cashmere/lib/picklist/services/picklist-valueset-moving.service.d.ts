import { PicklistFilterService } from './picklist-filter.service';
import { PicklistPaneComponent } from '../pane/picklist-pane.component';
import { FilterableSelectList, PicklistValueOptions, ValueListOption, ValueSetListOption } from '../pane/picklist-pane.model';
import { PicklistStateService } from './picklist-state.service';
export declare class PicklistValuesetMovingService {
    private filterService;
    private stateService;
    readonly valueList: FilterableSelectList<ValueListOption>;
    readonly valueSetList: FilterableSelectList<ValueSetListOption>;
    constructor(filterService: PicklistFilterService, stateService: PicklistStateService);
    moveOutValuesets(optionsToMove: PicklistValueOptions, pane: PicklistPaneComponent, shouldBreakValuesets?: boolean): void;
    private breakValueset;
    private moveSubValues;
    /**
     * Handles complex logic for when one pane is paged, and we want to keep the "x of y" counts accurate without a round trip to the server
     */
    private removeValuesFromPane;
}
